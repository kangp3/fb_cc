# Real config.yml file
config/config.yml

# Shut VIM up
*.swp

# D3 example stuff that shouldn't even be here in the first place
d3examples/
curvedgraphfiles/

# Local user icon directory
user_icons/
